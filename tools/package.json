{
  "name": "tools",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/*.browser.mjs"
  },
  "dependencies": {
    "csv-parse": "7.0.2",
    "playwright": "^1.62.1"
  },
  "private": true,
  "description": "维护性辅助工具（Apple 歌单导出）；不属于纯 Python 工作流本体。"
}
