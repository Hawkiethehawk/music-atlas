{
  "name": "music-atlas-web",
  "version": "0.1.0",
  "private": true,
  "description": "Music Atlas · Editorial Atlas Edition · 本机固定端口静态服务 http://127.0.0.1:8420",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/*.browser.mjs"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
