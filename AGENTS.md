# Music Atlas 仓库规则

## 1. 仓库边界

- 本目录（`apps/music-atlas`）是**独立 Git 仓库**，默认远端为 `origin` = `https://github.com/Hawkiethehawk/music-atlas.git`，分支 `master`。
- 获得明确推送授权后，目标为 GitHub `origin`；原 Gitee 远端 `gitee-archive` 仅作归档。部署授权不等于 Git 推送授权。
- 外层 Codex 工作区仓库（`E:\LLM-Sandbox\Codex`）不管理本目录内容；在外层仓库执行 `git add`/`commit`/`push` 时不得纳入本目录。
- 所有提交、推送、标签操作仅针对本仓库；跨仓库操作前必须重新确认仓库根（`git rev-parse --show-toplevel`）。

## 2. 版本与发布规则

- 本仓库未配置版本对象：**不设置版本号，不创建版本标签**。
- 提交与推送统一走 `version-manager` Skill；推送前必须更新本仓库 `CHANGELOG.md`（记录日期、实际变更、真实验证结果），并创建 `patch-YYYYMMDD-HHMMSS` 维护标签，标签在提交前确定并写入 CHANGELOG。
- 变更分类按 version-manager 规则执行；混合变更按其中影响最大的类型记录。
- 未获用户当前对话明确授权，不得 push、打标签或改写历史。

## 3. 数据边界（禁止提交）

- 以下内容属于个人数据或本地运行产物，已被 Git 忽略，暂存时必须排除：
- `input/`：个人歌单输入
- `runtime/`：本地运行产物与归档
- `styles/artist_style_profiles.json`：私有逐艺人风格画像（公开示例 `styles/artist_style_profiles.example.json` 可以提交）
- `secrets/`、`.env`：Apple Music 凭据与令牌
- `state.json`、`*.log`、`__pycache__/`
- 暂存使用明确路径，不使用覆盖整个仓库的 `git add -A`；提交前核对暂存区不含上述文件。

## 4. 设计边界（代码改动约束）

- 修改代码时保持以下边界：
1. Step 1/2/3 使用 JSON 契约衔接。Step 2 以本次 `PlaylistSnapshot` 和实际取得的公开资料为依据；Step 3 只消费本次已验证的分析包与可核对的候选证据。
2. 新结果使用 `sourced_tags_v1`：取得单曲标签才可归为单曲风格；专辑和艺人标签分别标为背景，不折算成单曲分类或听感。未取得可靠资料的内容保持未知，不凭 Agent 记忆、构造的 URL 或推测补齐。
3. 原歌单达到 1000 首时，只做歌手层级分析，即使用户只选择处理其中一部分；以原歌单数量选模式，以本次处理数量计算覆盖率。未达到 1000 首才查询单曲资料，并允许附带专辑、艺人背景。新来源模式不得把歌手背景写成逐首评价，也不得使用八轴评分或能量弧。
4. 候选来源、身份、排除规则与策略配比均须核对。`recommender.py` 按当前策略确定性选曲、去重和排序；Agent 不提交无来源的评分或试听结论。
5. 平台个性化推荐、登录态和历史运行结果不参与候选发现、排序或说明生成；个性化音乐页面不能作为证据来源（`_source_is_forbidden_personalization` 强制）。反馈只用于只读离线评估与人工批准的调优建议（`approval_required: true`），不自动调整排序。
6. 数量契约 `declared_track_count == track_count == len(tracks)` 由 `contracts.py` 强制。Schema 1.0 历史产物不可复用，只允许经 `archive-schema1` 归档。

## 5. 验证方式

- 提交前实际执行以下检查；准备推送时，再按 `version-manager` 规则把真实验证结果写入 CHANGELOG：

```bash
python -m unittest discover -s tests -v
python -m compileall -q .
```

- 涉及工作流行为改动时，另跑仓库夹具端到端链路：

```bash
python workflow.py run --input tests/fixtures/playlist_sample.json --reader local_json \
  --platform apple_music --playlist-id sample --playlist-name '示例歌单' --runtime-dir runtime/local-run
python workflow.py agent --analysis runtime/local-run/musician_analysis.json \
  --prompt runtime/local-run/agent_prompt.md --output runtime/local-run/recommendation_bundle.json \
  --report-output runtime/local-run/report.txt --command 'python tests/fixtures/fake_agent.py'
```

- 不得把未运行的测试写成已通过。

### 真实歌单并行验收

- 每份歌单跨三轮保持同一个固定链接，三条并行算一轮，至少完整执行三轮（共至少 9 次首次完整运行）。每次从原始公开链接和全新隔离目录启动，不复用快照、分析包、候选池、推荐历史或上轮结果；这里的首次指本地业务冷启动，第三方响应缓存状态单独记录，无法识别时标为未知。任一轮任一歌单失败、超时或未达到完整 `completed`，最终验收不通过；修复后从第一轮重新计数。
- 涉及首次运行、Step 2/3、断点续跑、候选路线、严格配比或总耗时的修改，除单元和浏览器夹具测试外，并行启动三个真实歌单测试：Apple Music `pl.u-9DU1g31kdJ`（约 123 首）、网易云 `18135667715`（约 91 首）、网易云 `7786449876`（约 1917 首）。真实账号和歌单不得写入仓库；通过本机未入库环境变量 `ATLAS_USER`、`ATLAS_PASS` 与 `ATLAS_REGRESSION_PLAYLISTS_JSON` 提供回归配置。优先从当前账号保存的公开链接只读取得 URL；链接失效时暂停该轮验收并向用户索取替代链接。不得复制凭据。
- 三个任务分别使用独立临时运行目录和测试用 `MUSIC_ATLAS_USER_ID`，并行启动、记录各自的启动时间与重叠区间。不得指向正式 `current-data`、正式 `web-jobs` 或用户推荐历史；不得覆盖线上 Atlas。测试密钥只通过既有服务环境注入，日志不得输出密钥。
- 验证 1917 首歌单时，须证明原歌单规模触发歌手层级模式；即使选择处理 50%，也不能转为逐曲模式。有来源的歌手所对应的本次处理曲目数是覆盖分子，处理曲目数是分母，按当次策略 `min_artist_weight_share` 判断。另两份歌单将每首曲目按“单曲标签→仅有专辑背景→仅有艺人背景→未知”的优先级只计入一类；只有前两类合计进入 `min_track_or_album_share` 发布门槛，后三类均不得计入单曲分类。当前默认下限分别为 30% 和 50%，以实际策略为准。
- 每条任务完成时立即以该任务的测试账号并行回读隔离测试环境的最终结果，分别记录从原始链接首次提交到回读成功的墙钟耗时；不得等其余任务结束后才回读，也不得把内部完成时间当作回读时间。三组最终曲目、音乐风格分析与选曲硬约束均通过才算 `completed`，不以每次运行的独立审计或单列本地复核为完成前提。每条歌单都必须在 120 秒内达到完整终态；临时预览、仅锁定曲目、失败或超时均不算通过。资料缺口允许保留，但必须逐项如实显示，不得声称未执行的独立审计已通过。首次运行的 120 秒结果不得用续跑、跨运行复用或上一轮的本地缓存、产物替代；第三方响应缓存命中单独记录。
- 对隔离目录中的最终结果逐组检查目标曲目数、候选类型的当前策略配比、跨组三十首唯一身份、原歌单与近期推荐排除、真实平台歌曲 URL、身份与来源校验、资料缺口显示及账户隔离。摘要模式按其实际策略验收，不套用逐曲模式的 4/3/2/1。失败或超时保留隔离目录的事件和错误供定位；修复后可单独重跑定位，但最终验收须重新并行运行三条歌单共三轮，记录各自耗时及重叠区间，不能将并行负载结果表述为独占运行耗时。

## 6. 其他约定

- 默认使用中文回答与书写文档；CHANGELOG 与提交说明使用中文。
- 语言强制、执行确认、PowerShell（`pwsh`）等通用规则继承外层 `E:\LLM-Sandbox\Codex\AGENTS.md`。
- 生产部署前置条件：完成一次使用外部可验证证据的实时代理试运行（见 README「生产就绪前置条件」）；部署相关操作需单独授权。

## 7. Agent 工作与需求澄清

### 7.1 总体原则

- 先理解，再实现；优先确保准确理解用户真实意图，不以盲目追求速度为目标。
- 需求存在歧义、缺少关键信息，或不同选择会明显影响结果时，主动向用户提问；不得自行猜测、补全或虚构需求。
- 能从代码、文档、配置、测试或已有上下文可靠确定的信息，先自行调查。
- 影响较小的细节可以采用合理默认值，但应说明假设；影响较大的事项必须确认。
- 复杂任务先完成需求对齐，再正式实现；不要为了减少提问而擅自决定重要需求。

### 7.2 开始任务前

- 对于非简单任务，在正式修改代码或执行大量工作前，必须：
  1. 阅读相关代码、配置、文档、测试和现有实现。
  2. 理解当前系统的工作方式。
  3. 总结用户目标和预期结果。
  4. 找出未知信息与歧义，并判断其是否会实质影响实现方式或结果。
  5. 对重要歧义先提问，获得必要信息后再实现。
- 不要在尚未调查现有实现时，询问本可由项目自行确定的问题。

### 7.3 先查代码，再问用户

- 优先自行确认以下内容：
  - 文件位置、目录结构、类名、函数名和变量名。
  - 项目使用的框架、库、代码风格和既有设计模式。
  - API 调用方式、配置内容、测试已定义的行为和文档约束。
  - 代码库中可以明确判断的业务逻辑。
- 只有在现有项目无法可靠确定时，才向用户询问。

### 7.4 需要主动澄清的情况

- 出现以下任一情况时，优先向用户确认：
  - 最终目标、预期结果或验收标准不明确。
  - 存在两种以上合理解释，或多个方案会明显影响体验、业务逻辑、维护成本、架构或结果。
  - 未明确适用的用户、页面、场景、触发条件、流程或交互方式。
  - 数据来源、结构、存储、同步、更新频率、权限、隐私或访问范围不明确。
  - 边界条件、异常处理、失败重试、重复操作、无数据、网络失败、接口失败或权限拒绝的行为不明确。
  - 平台、系统版本、设备、兼容性或旧数据/旧逻辑的保留范围不明确。
  - 用户描述与现有代码行为冲突，或用户要求之间互相冲突。
  - 即将进行不可逆、高风险、成本高或后续修改代价大的操作。
  - 进一步询问能够明显提高结果准确性。

### 7.5 提问方式与分批提问

- 问题应简洁、具体，并能直接影响下一步决策，尽量降低回答成本。
- 存在典型方案时，提供 2～4 个明确选项；可给出推荐及原因，并允许用户提出其他方案。
- 示例：
  - “这个功能需要对所有用户生效，还是只对新用户生效？”
  - “请求失败时选择 A. 自动重试、B. 显示错误、C. 静默失败？”
- 复杂需求按以下顺序分批确认：
  1. 目标。
  2. 使用场景。
  3. 核心行为。
  4. 交互方式。
  5. 数据与状态。
  6. 边界情况。
  7. 异常处理。
  8. 技术方案。
  9. 验收标准。
- 第一轮回答会决定后续问题时，先问第一轮；不要一次抛出大量互相依赖的细节问题。

### 7.6 产品功能任务检查项

- 新功能、产品需求或 UI 功能应主动检查：
  - 用户与场景：谁使用、何时触发、从哪里进入、使用频率及目标用户群体。
  - 功能行为：用户操作、系统响应、自动行为及需要用户确认的行为。
  - UI/UX：展示位置、默认状态，以及 loading、empty、error、提示、弹窗、Toast、通知、确认、返回、取消和关闭行为。
  - 数据：来源、本地或服务端保存、跨设备同步及更新频率。
  - 边界：无数据、网络失败、接口失败、权限拒绝、重复操作和异常数据。
  - 兼容性：Android/iOS/Web 范围、最低系统版本、旧版本数据和旧逻辑。
  - 验收：完成条件、必测场景，以及性能、稳定性和数据指标要求。
- 其中任何未明确项只要会明显影响实现，就应先询问。

### 7.7 技术实现任务检查项

- 存在多个合理技术方案时，先比较其是否影响以下内容：
  - 现有架构、依赖、数据结构、数据库迁移、API、公开接口和兼容性。
  - 性能、安全、其他模块和长期维护成本。
- 方案差异较大时，说明主要方案、取舍和影响，让用户选择。
- 只是内部实现细节且不影响最终结果时，沿用项目现有规范自行决定。

### 7.8 假设规则

- 仅在细节影响较小、容易修改、有明确行业惯例或项目已有一致模式时采用默认值，并简要说明假设。
- 不得自行假设以下内容：
  - 核心业务规则、用户权限、收费逻辑。
  - 删除、覆盖、数据迁移等重要数据行为。
  - 用户可见的重要行为、安全行为、不可逆操作。
  - 大规模架构调整或会显著增加维护成本的方案。

### 7.9 用户要求与现有实现冲突

- 不得静默选择现有实现或新描述中的一方。
- 发生冲突时，应明确说明：
  - 当前代码的实际行为。
  - 用户描述的目标行为。
  - 两者的具体冲突。
  - 询问保留现有逻辑还是修改为新逻辑。

### 7.10 实现过程中的持续澄清

- 需求澄清贯穿整个任务。
- 若发现以下情况，应暂停相关决策，说明影响并询问：
  - 新的需求冲突。
  - 隐藏业务规则。
  - 架构限制。
  - 会导致行为变化的技术问题。
  - 原方案无法达成预期。
  - 需要用户未授权的重要取舍。
- 不得为了继续执行而擅自扩大范围。

### 7.11 避免过度提问

- 不要询问以下问题：
  - 低影响细节。
  - 可由代码确定的信息。
  - 项目默认规范。
  - 用户已回答的问题。
  - 当前上下文已有答案的问题。
- 目标不是最大化提问数量，而是最大化需求理解准确度。

### 7.12 沟通与决策准则

- 直接说明问题及其对实现的影响，优先给出具体选项，保持上下文连续。
- 按以下准则处理不确定性和方案选择：
  - 能从项目确定：自行调查并采用现有实现。
  - 无法确定但影响小：合理假设并说明。
  - 无法确定且影响大：提问确认。
  - 存在多个重要方案：说明取舍和影响，让用户选择。
  - 高风险、不可逆、成本高：必须确认。
- 不要默默创造需求，不要为了自主完成任务而牺牲需求准确性。
- 复杂任务中，只要补充信息能明显改善结果，就主动提问。
- 执行原则：
  > 先理解，再实现。先查代码，再问用户。能自行可靠确定的，不问；低影响的不确定性，可以合理假设；高影响的不确定性，必须确认。
